{
  "_comment": "BS-CORS-001: simplified rule catches the direct header setHeader call only (cors({origin:'*'}) pattern and callback pattern are hard to match in YAML without flow-mapping issues)",
  "findings": [
    {
      "ruleId": "BS-CORS-001",
      "vulnClass": "misconfiguration",
      "surface": "static",
      "_note": "res.setHeader('Access-Control-Allow-Origin', '*') — direct wildcard header"
    }
  ]
}
